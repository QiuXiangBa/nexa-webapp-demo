# 移动端测试指南

## 本地测试

### 1. 启动开发服务器

```bash
npm run dev
```

### 2. 在移动设备上测试

#### 方法一：使用手机浏览器访问

1. 确保手机和电脑在同一局域网
2. 查看终端输出的本地 IP 地址（如：`http://192.168.x.x:5173`）
3. 在手机浏览器中访问该地址

#### 方法二：使用 Chrome DevTools

1. 打开 Chrome 浏览器
2. 按 `F12` 打开开发者工具
3. 点击设备工具栏图标（或按 `Ctrl+Shift+M`）
4. 选择设备型号（如 iPhone 12 Pro）
5. 刷新页面查看效果

#### 方法三：使用微信开发者工具

1. 打开微信开发者工具
2. 选择"公众号网页"
3. 输入本地开发地址
4. 查看移动端效果

## UI 视觉回归基线（360/390/430）

### 运行回归（比对已有基线）

```bash
npm run e2e:visual
```

### 首次生成 / 更新基线截图

```bash
npm run e2e:visual:update
```

### 基线说明

- 覆盖页面：
  - 首页 `/home`
  - 交易中心 `/trade`
  - 商家地图 `/merchant-map`
  - 我的 `/me`
  - 我的钱包 `/me/wallet`
  - 消费收益详情 `/home/consumer-income-detail`
  - 中证收益详情 `/home/zspace-income-detail`
- 截图宽度：`360 / 390 / 430`
- 设计高度基准：`844`
- 测试会固定 `Date.now`，避免 mock 时间字段引起截图抖动
- 快照目录：`tests/e2e/ui-visual-baseline.spec.ts-snapshots/`
- 评审建议：PR 中同时查看三档截图差异，避免只在 390 通过

## 测试检查清单

### 布局适配
- [ ] 页面在不同屏幕尺寸下正常显示（375px、414px、768px 等）
- [ ] 文字大小适中，不会过大或过小
- [ ] 图片按比例缩放，不变形
- [ ] 底部导航栏固定在底部
- [ ] 安全区域适配正常（iPhone X 及以上机型）

### 功能测试
- [ ] 首页搜索框可以输入
- [ ] 快捷功能按钮可以点击
- [ ] 轮播图可以滑动切换
- [ ] 品牌卡片列表正常显示
- [ ] 数域页面数据正常加载
- [ ] 底部导航栏切换正常

### 交互测试
- [ ] 点击响应及时
- [ ] 滑动流畅
- [ ] 页面滚动正常
- [ ] 没有出现横向滚动条

### 性能测试
- [ ] 页面加载速度合理
- [ ] 图片加载优化
- [ ] 路由切换流畅

## 常见问题

### 1. 页面显示过大或过小

检查 `vite.config.ts` 中的 `viewportWidth` 配置，确保与设计稿宽度一致（375px）。

### 2. 底部导航栏被遮挡

检查页面是否有足够的 `padding-bottom`，确保内容不被 TabBar 遮挡。

### 3. 安全区域适配问题

确保 `index.html` 中的 viewport meta 标签包含 `viewport-fit=cover`。

## 真机测试建议

1. **iOS Safari**：测试主要功能
2. **Android Chrome**：测试兼容性
3. **微信内置浏览器**：测试实际使用场景
4. **不同屏幕尺寸**：iPhone SE、iPhone 12、iPhone 14 Pro Max 等
